int redPin = 11;
int greenPin = 10;
int bluePin = 9;
int energyRed = 255;
int energyGreen = 255;
int energyBlue = 255;
int energyAll = 255;
String comando;

void setup() {
  // put your setup code here, to run once:
  Serial.begin(9600);
  pinMode(redPin, OUTPUT);
  pinMode(greenPin, OUTPUT);
  pinMode(bluePin, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:
  while (Serial.available() == 0){

    }
  comando = Serial.readStringUntil('\r');

  if (comando == "rojo")
     analogWrite(redPin, energyAll);
     analogWrite(greenPin, 0);
     analogWrite(bluePin, 0);

  if (comando == "verde")
     analogWrite(redPin, 0);
     analogWrite(greenPin, energyAll);
     analogWrite(bluePin, 0);
     
  if (comando == "azul")
     analogWrite(redPin, 0);
     analogWrite(greenPin, 0);
     analogWrite(bluePin, energyAll);

  if (comando == "turquesa")
     analogWrite(redPin, 0);
     analogWrite(greenPin, energyAll);
     analogWrite(bluePin, energyAll);

  if (comando == "morado")
     analogWrite(redPin, energyAll);
     analogWrite(greenPin, 0);
     analogWrite(bluePin, energyAll);

  if (comando == "amarillo")
     analogWrite(redPin, energyAll);
     analogWrite(greenPin, energyAll);
     analogWrite(bluePin, 0);
  
    if (comando == "on")
     analogWrite(redPin, energyAll);
     analogWrite(greenPin, energyAll);
     analogWrite(bluePin, energyAll);

  if (comando == "off")
     analogWrite(redPin, 0);
     analogWrite(greenPin, 0);
     analogWrite(bluePin, 0);
  if (comando == "")
      Serial.println("Comando incorrecto, intentelo de nuevo");
}
